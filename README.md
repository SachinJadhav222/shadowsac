# what is this
This is JS shadow package 

# installation
npm i shadowsac --save

Then....

```
import {shadowsac} from 'shadowsac;

shadowsac({
    shadow_type: 'soft',
    padding: false
})

```
# Options 